### Notarized builds

ClaudeBar is now signed with an Apple Developer ID certificate and notarized
by Apple. macOS no longer shows a Gatekeeper warning on first launch.

No action needed — this update installs like any other.
