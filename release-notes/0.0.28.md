### "100%" no longer wraps in the menu bar

At exactly 100% usage the menu bar label broke onto two lines, showing a
clipped "100" stacked over "%". The item is now measured the way the system
lays the text out, so a full bar reads as one line.
